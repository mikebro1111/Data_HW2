version: '3.8'

services:
  airflow-webserver:
    image: apache/airflow:2.1.0
    # Configuration for the webserver...

  airflow-scheduler:
    image: apache/airflow:2.1.0
    # Configuration for the scheduler...

  # Additional base configurations for other services...

